# Playwright E2E

The stable selectors `ship-to`, `add-product`, `product-search`, and `confirm-products` are present for the requested order workflow. Install `@playwright/test` in this folder and run the scenarios against the documented local API/web commands. Full browser automation remains an integration-environment gate because browser binaries are not vendored in this repository.
